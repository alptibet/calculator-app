{
  "name": "calculator-app",
  "version": "1.0.0",
  "description": "Calculator",
  "scripts": {
    "devserver": "live-server --no-browser",
    "watch:sass": "sass --no-source-map --watch src/sass/main.scss:css/style.css",
    "start": "npm-run-all --parallel devserver watch:sass"
  },
  "author": "Alp Tibet",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.35.1"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
